<%= "最近登録の部屋、#{session[:room]}です。" if session[:room] %>
<p id="notice"><%= notice %></p>

<h1>予約管理のページへようこそ</h1>
<p>本日のアクセス時間: <%= Time.now.strftime("%Y年%m月%d日%H時%M分") %></p>

<table>
  <thead>
    <tr>
      <th><%= t 'activerecord.attributes.room.name' %></th>
      <th>Place</th>
      <th>Number</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rooms.each do |room| %>
      <tr>
        <td><%= room.name %></td>
        <td><%= room.place %></td>
        <td><%= room.number %></td>
        <td><%= link_to 'Show', room %></td>
        <% if current_user.admin %>
          <td><%= link_to 'Edit', edit_room_path(room) %></td>
          <td><%= link_to 'Destroy', room, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Room', new_room_path, class:"new-button" %>
